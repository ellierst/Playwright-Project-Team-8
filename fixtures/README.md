Fixtures

The fixtures/ folder contains custom Playwright fixtures used to extend the default test context and provide reusable setup logic across multiple tests.

Fixtures allow you to:

Extend the base test object
Share common setup (e.g., authentication, page objects)
Reduce duplication in test files
Improve test readability and maintainability